# API Configuration
VITE_API_URL=http://localhost:4000

# docker-compose.yml (legacy MongoDB-based backend stack) — only needed if you run docker-compose
MONGO_ROOT_USERNAME=
MONGO_ROOT_PASSWORD=
MONGODB_URI=
RESEND_API_KEY=
EMAIL_FROM=
EMAIL_USER=
JWT_SECRET=
